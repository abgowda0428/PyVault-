# Practise Numpy in Python
import numpy as np

print(np.__version__)

my_list = [1,2,3,4]

# Conveting the List into Array
my_aarry = np.array(my_list)
print(type(my_aarry))

# Generating the Matrix with All Pre-Defined Values or intial Empty Array
zero_matrix = np.zeros((3,3))
print(zero_matrix)

# Pre-Defined one Matrix
oneDMatrix = np.ones((3,3))
print(oneDMatrix)

